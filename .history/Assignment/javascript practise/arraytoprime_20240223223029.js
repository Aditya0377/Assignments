const arr = [4, 6, 8, 9];
let k = +prompt('enter the range end');
arr.forEach(toprime())
function toprime(index) {
    for (let i = 2; i < index; i++) {
        if (index % i == 0) {
            a[index] + i
        }
    }
}

